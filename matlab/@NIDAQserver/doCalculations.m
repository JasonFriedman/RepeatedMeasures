% DOCALCULATIONS - do some calculations on the data, which can be requested from the client
%
% dataSummary = doCalculations(s,data)
%
% data is the last recorded data, it returns a struct

function dataSummary = doCalculations(d,data)

% for now, do nothing

dataSummary.toSave = data;

