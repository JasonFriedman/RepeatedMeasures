% GETDATALENGTH - get the length of a single frame of data
%
% datalength = getdatalength(gs,~)
%
% The files are saved on the gopro, so no data is saved on the computer
% Set to 1 to avoid empty matrices

function datalength = getdatalength(gs,~)

datalength = 1; 
