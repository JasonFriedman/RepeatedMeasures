% CHECKRECORDING - check recording on this device (whether the data was OK)
% Generally, it will print an error message on the screen if there is a
% problem (e.g. missing markers)

function checkRecording(gfc,e,dataSummary,experimentdata,thistrial)

checkRecording(gfc.glove,e,dataSummary,experimentdata,thistrial);
checkRecording(gfc.tracker,e,dataSummary,experimentdata,thistrial);
